{
  "name": "opencred",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "rimraf node_modules dist"
  },
  "devDependencies": {
    "turbo": "^1.11.5",
    "typescript": "^5.5.4"
  }
}
