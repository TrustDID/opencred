# Scripts Directory

This directory is reserved for project support scripts such as repository setup, release helpers, or contributor automation.

Currently, the monorepo is scaffolded for open-source contributions, and scripts will be added as issues when builder automation is defined.
