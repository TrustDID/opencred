# OpenCred Web

This Next.js app is the frontend scaffold for OpenCred.

## Structure

- `pages/index.tsx` — home page scaffold.
- `pages/_app.tsx` — application wrapper.
- `styles/globals.css` — global styles.

## Notes

This scaffold is intentionally lightweight. UI and application flows will be implemented by contributors once API contracts and backend endpoints are defined.
