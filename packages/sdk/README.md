# OpenCred SDK Package

This package will contain reusable SDK utilities for OpenCred.

Structure:
- `types/` — shared TypeScript types for credential payloads, response shapes, and Stellar objects.
- `utils/` — helper functions for common serialization, validation, and SDK workflows.

The SDK is currently a scaffold for contributor-driven implementation.
