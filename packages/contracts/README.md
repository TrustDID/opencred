# OpenCred Contracts Package

This package contains Soroban smart contract scaffolding for the OpenCred monorepo.

## Structure

- `credential_registry/` — Rust-based Soroban contract scaffold for credential registry behavior.

The contract package is intentionally stubbed so contributors can design and implement the contract interfaces and storage layout through issues.
