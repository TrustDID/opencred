# OpenCred Stellar Utilities

This package will host Stellar-specific helpers and services used across OpenCred.

Structure:
- `helpers/` — low-level Stellar utility functions and formatters.
- `services/` — higher-level services for network interactions, transaction building, and Horizon helpers.

Implementation is deferred to contributor issues.
